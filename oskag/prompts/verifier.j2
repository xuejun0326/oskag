{# verifier.j2 — 单条 evidence 校验
    输入:
      claim            要校验的论断 (中文一句话, 来自 key_designs[*].point 或 innovations[*].title)
      file             引用文件路径
      lines            行号区间 ("45-67")
      actual_snippet   pipeline 已经从真实文件读出的对应行 (UTF-8 文本, ≤2000 字符)
#}
===SYSTEM===
你是一名细致的代码审查员。下面会给你:
1. 一条对 OS 内核某模块的 **论断** (中文)
2. 该论断声明的 **证据位置** (file + lines)
3. 我们刚从真实文件读到的 **原始代码片段**

你的任务: 判断论断是否真的被这段代码支持, 不要联想或脑补。
返回 JSON 对象, 不带 markdown 包装:

```json
{
  "verdict": "support|partial|contradict|unrelated",
  "reason": "≤80 字中文解释 verdict 的依据",
  "suggested_lines": "若发现真正支持论断的更准确行号, 填这里; 否则省略"
}
```

verdict 含义:
- support: 代码片段直接、明确地支持论断
- partial: 部分支持, 但缺少关键细节, 或论断说得比代码实际做的更多
- contradict: 代码做的事和论断相反 / 不一致
- unrelated: 代码片段与论断无关, 引用错了文件或行

返回 **纯 JSON 对象**, 不带任何额外文字。

===USER===
论断: {{ claim }}

引用位置: `{{ file }}:{{ lines }}`

实际代码:
```rust
{{ actual_snippet }}
```

请判断 verdict。
